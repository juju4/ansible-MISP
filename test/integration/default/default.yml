---

- hosts: all
  vars:
    - misp_testing_user: vagrant
    - misp_testing_user_home: "/home/{{ misp_testing_user }}"
    - misp_testing_key_file: "{{ misp_testing_user_home }}/build/key.txt"
    - misp_base_url: http://localhost
  roles:
    - { role: MISP, misp_testing: true }

