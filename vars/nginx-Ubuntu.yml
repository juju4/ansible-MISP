---

webserver: "{{ misp_webserver_nginx }}"
apache_svc: nginx
www_user: www-data

apache_sitedir: /etc/nginx/sites-available

misp_services: "{{ misp_services_nginx }}"

php_ini: "{{ php_ini_nginx }}"
